# File:.env
# GCP/BigQuery Settings
GCP_PROJECT_ID=your-project-id
DBT_DATASET=your_analytics_dataset
DBT_LOCATION=US

# Model Context Protocol (MCP) Settings
# Required if you want the AI to query your dbt project context
DBT_HOST=cloud.getdbt.com
DBT_TOKEN=your_access_token
DBT_PROD_ENV_ID=12345
DBT_USER_ID=67890

# Local path for bind mounts (usually current directory)
DBT_PROJECT_DIR=./